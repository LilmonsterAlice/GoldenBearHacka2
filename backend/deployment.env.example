# Optional variables for backend/compose.integration.yml; no credentials.
# Person 5 may merge these settings into the root environment template.
ANALYSIS_MODE=mock
ANALYSIS_HOST_PATH=./backend/fixtures/analysis.stage1.mock.json
CORS_ORIGINS=http://localhost:3000
CHAT_SERVICE=
CHAT_TIMEOUT_SECONDS=20

# After Person 1 delivers an approved candidate, set:
# ANALYSIS_MODE=real
# ANALYSIS_HOST_PATH=./generated/analysis.json

# After Person 4 provides the async callable, set:
# CHAT_SERVICE=backend.services.llm:answer_chat
# Obtain actual API/MCP transport and credential settings from Person 4.
